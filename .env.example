VITE_API_URL=http://localhost:4000/api/v1
# Error tracking (Sentry). Leave unset locally — Sentry stays fully disabled.
# Get the DSN from the jobtrail-fe Sentry project: Settings → Client Keys (DSN).
VITE_SENTRY_DSN=
