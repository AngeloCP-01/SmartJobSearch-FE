# Frontend Tracker (v1)

Status legend: ☐ Not started · ◐ In progress · ☑ Done · ⛔ Blocked

**Last updated:** 2026-06-26
**Master tracker:** `../TRACKER.md`

| ID | Milestone | Depends on | Status | Notes |
|----|-----------|-----------|--------|-------|
| FE-0 | Scaffold | — | ☑ | Vite + Tailwind v4 + Vitest + MSW; Plus Jakarta Sans + sky/green palette |
| FE-1 | Auth flow | BE-1 | ☑ | login/register, in-memory token, 401→refresh interceptor, route guard, sidebar layout |
| FE-2 | Companies | BE-2 | ☑ | list + search + create + delete |
| FE-3 | Applications Kanban + List | BE-3 | ☑ | @dnd-kit board, status-colored columns, optimistic status moves. **+ List view** (2026-06-25): `Board\|List` toggle (persisted), sortable table with inline status quick-change. |
| FE-4 | Interviews | BE-4 | ☑ | list + create (application/type/interviewer) |
| FE-5 | Dashboard | BE-5 | ☑ | totals, by-status, upcoming interviews cards |
| FE-6 | Deploy | BE-6 | ☑ | **Live** 2026-06-25. FE on Vercel (https://smart-job-search-fe.vercel.app), API on Render (https://smartjobsearch-api.onrender.com/api), Postgres on Neon, uploads on Supabase Storage. Verified: register/login + cross-site refresh + uploads. |

## v1.5 — Application Details ☑ (2026-06-23, merged to `main`)
Right-side drawer to view/edit/create an application with all fields; company picker with **inline create**; the application's **interviews** listed with add/delete; Kanban cards show **company name + salary chip**; New-application + open-card buttons. Focus-trapped dialog. (Backend side: `company` included on application responses + unlink — in the BE repo.)

## v2 — Contacts (frontend slice) ☑ (2026-06-23, merged to `main`)
New **Contacts** sidebar page (searchable list, follow-up pill) + `ContactDrawer` (create/edit/delete, company picker w/ inline create, email/URL guards). New **Contacts section** in the application drawer (link existing / quick-create / unlink). `src/api/contacts.js`; query keys `['contacts']` / `['application', id]`. Spec in BE repo; plan: `docs/superpowers/plans/2026-06-23-contacts-frontend.md`.

## v2 — Post-Contacts UX + fixes ☑ (2026-06-23, merged to `main`)
- **Applications board:** removed the redundant quick-add; "Position title" input is now a **board search filter**; "New application" (drawer) is the single create path.
- **Application cards:** show the **applied date**; whole card body opens the detail drawer on click (guarded against drag / keyboard-synthesized clicks); ↗ button stays the keyboard affordance.
- **Auth resilience:** single-flight refresh (dedupes concurrent 401 refreshes); refresh only force-logs-out on a real `401` (a server outage/5xx no longer logs you out); **"Keep me logged in"** checkbox on Login (checked by default) → sends `rememberMe`.
- **Errors:** drawers surface the backend's per-field validation `details` (`src/lib/apiError.js`).

## Tests
62 passing across 13 suites (adds Analytics page tests — metrics, loading, empty, error, chart presence/absence — + the Analytics nav-link test). Production build verified.

## v2 — Analytics (frontend slice) ☑ (2026-06-23, merged to `main`)
New dedicated **Analytics** sidebar page (`/analytics`, placed right after Dashboard, `LineChart` icon) consuming `GET /api/analytics`. Four headline metric cards (total / interview / offer / rejection, rates as `%`) reusing the Dashboard card look, plus two **Recharts** charts: a horizontal **Pipeline** bar (count by status, per-status colors from DESIGN.md) and an **Applications over time** area chart (12 months). Loading / empty ("Add applications to see analytics") / error states. `src/api/analytics.js`; query key `['analytics']`; `recharts` added; jsdom `ResizeObserver` stub in `src/test/setup.js`. Spec in BE repo; plan: `docs/superpowers/plans/2026-06-23-analytics-frontend.md`. **Reviewed (read-only subagent) — Approved, no blocking;** non-blocking polish in `b556447` (gated the error banner on `!data` so loading/error/content stay mutually exclusive on a failed background refetch).

## In Flight
_v2 complete; **v3 in progress** — Documents (V3-1) + Activity Log (V3-2) + Résumé Analysis (V3-3) + résumé-analysis **OpenRouter LLM layer (V3-4)** merged to `main`. Plus a **details-drawer UX pass** (job-description read view, salary/source polish, `2026-06-25`). **138/138 tests** + build verified. **FE-6 Deploy DONE — full stack is live** (Vercel + Render + Neon + Supabase) + a **portfolio-readiness pass** (demo, landing, README/CI, perf) + an **AI cover-letter generator** (with Save-to-Documents). See root `../TRACKER.md` for the full module status._

## Notes / Blockers
- 2026-06-26 — **Cover-letter follow-ups: smart filename + Save-to-Documents** (FE `24d3e73`/`6f92838`/`5777537`, BE `0f02467`). (1) The download `.txt` is named after the application: `"<position> - <company>-cover-letter.txt"` (filename-unsafe chars stripped; company omitted when it's the server's `the company` placeholder; falls back to `cover-letter.txt`). Pure exported `coverLetterFilename(position, company)`, unit-tested. (2) **Save to Documents** — saves the (editable) letter as a `.txt` **CoverLetter** document (`Cover Letter — <position>`) and **links it to the application**, so it appears in Documents and under the app's linked docs; shows a "Saved" confirmation. Required widening the BE upload allow-list to accept `text/plain` (`documents.upload.js`; disallowed-type test switched to `image/png`, added a `.txt` acceptance test). **Verified live**: `.txt` upload (201) → link (201) → cleanup (204). FE save-flow tested via mocked `createDocument`/`linkDocument` (jsdom doesn't resolve multipart cleanly; the real upload path works — see Documents page). **FE 138 / BE 165 passing (1 skipped)**.
- 2026-06-26 — **AI cover-letter generator** (FE `b12df29`, BE `40b1520`). New `/cover-letter` page (sidebar, `PenLine`): pick an application + résumé → `POST /api/analysis/cover-letter` → a tailored letter shown in an editable textarea with **Copy** + **download .txt**; gated on AI availability, warns on missing JD. Backend reuses the OpenRouter engine — refactored to share `chat()` + a generic `withModelFallback()` between the JSON analysis and new freeform `generateText`/`generateTextWithFallback` (same retry/429-fast-fallback/Retry-After policy; all 21 engine tests stayed green). AI-only (no deterministic fallback): no JD → 400, no key / saturated → 503. **Verified live** — `gpt-oss-20b:free` produced a coherent, role-specific letter on the demo data. TDD: 4 BE + 2 FE → **FE 136 / BE 164 passing (1 skipped)**.
- 2026-06-26 — **Portfolio-readiness pass** (a batch to make the deployed app shine for reviewers). (1) **Demo experience** — a **"Try the demo"** one-click login (`3ceca88`) into a seeded sample account (backend `prisma/seed.js`), plus a keep-alive workflow so Render doesn't cold-start. (2) **Public landing page** at `/welcome` (`3d04d9e`) — hero + features + AI-analysis showcase + CTAs; `ProtectedRoute` now sends guests there; shared `useDemoLogin` hook reused by login + landing. (3) **README + screenshots + CI** (`e4cbea9`) — polished README with real screenshots captured from the live demo (`docs/screenshots/`), architecture, engineering highlights; **GitHub Actions CI** (Vitest + build) with a green badge. (4) **Perf + a11y** (`c938226`) — route-based **code-splitting** drops the initial bundle **761 KB → 284 KB** (Recharts/​@dnd-kit load on-demand; Suspense boundary in Layout); skip-to-content link + `<main>` landmark; Open Graph/meta tags for rich link previews. Responsive verified on mobile (390px). **134 tests**, CI green.
- 2026-06-26 — **Loading feedback across the app** (`8195968`). Now that the FE talks to a remote API with free-tier **cold starts**, in-flight requests are made visible three ways: (1) a **global top progress bar** in `Layout` driven by `useIsFetching() + useIsMutating()` — one place that covers every query/mutation app-wide (decorative/`aria-hidden`, `motion-reduce` fallback, `data-testid="top-progress"`); (2) a reusable `<Spinner>` replacing the bare `Loading…` text on all 8 pages + `ProtectedRoute`; (3) a `loading` prop on `<Button>` (spinner + disabled + `aria-busy`) used on the application-drawer Save/Delete. Keyframe `indeterminate` added to `index.css`. TDD: Spinner + Button-loading + bar show/hide → **131 total**.
- 2026-06-25 — **Applications Status + Company filters** (`058504c`). Two dropdowns next to search apply to **both** Board and List. Company options derived from loaded apps (no extra query, no empty choices). On the board a status filter narrows to just that column (`shownStatuses`); on the list it filters rows. A **Clear** link resets all filters/search when any is active; empty-state copy generalized to "…match your filters." TDD: status-filter + company-filter + clear → **125 total**. (Tightened two pre-existing `getByText('Acme')` assertions to `{ selector: 'p' | 'td' }` since company names now also render as filter `<option>`s.)
- 2026-06-25 — **Applications List view added** (`ff35b68`). A `Board | List` segmented toggle in the header, persisted to `localStorage` (`applicationsView`, default Board). **List** = sortable `<table>` — Position, Company, **Status (inline quick-change `<select>` reusing the same `moveMutationOptions` optimistic mutation as drag)**, Salary, Applied — with click-row-to-open + a keyboard `Maximize2` open button. Sortable headers via pure exported `sortApps` (default `applicationDate` desc). Board search now also matches **company** name, in both views. Status pills reuse `STATUS_STYLES`; table scrolls horizontally on narrow screens. TDD: `sortApps` unit + List-render + status-PATCH-from-list + persist-across-remount → **122 total** + build. (Also added `beforeEach(() => localStorage.clear())` in the Applications test so the persisted view doesn't leak between tests.)
- 2026-06-25 — **Documents upload UX** (`656c6dc`). Replaced the unstyled native file input with a click-or-drag dropzone showing the chosen file (icon/name/size/"click to replace") + Remove, drag-over highlight, keyboard accessible; prefills the document name from the filename. +1 test → 118.
- 2026-06-25 — **FE-6 Deploy DONE — full stack is LIVE (free tier).** FE→**Vercel** `https://smart-job-search-fe.vercel.app`, API→**Render** `https://smartjobsearch-api.onrender.com/api`, Postgres→**Neon** (5 migrations applied), uploads→**Supabase Storage** bucket `job_search_documents` (S3 driver smoke-tested live before deploy). Smoke-verified: register returns 201 + `Secure; SameSite=None` cookie (proves the prod cookie path), cross-site refresh, uploads. **Gotcha hit & resolved:** `CORS_ORIGIN` must be the bare origin `https://smart-job-search-fe.vercel.app` — it had been set to `…/login` (with a path), which never matches the browser's `Origin` header (scheme+host only), so every request was CORS-blocked and registration failed. Also: `NODE_ENV` must be lowercase `production` (the code's `=== 'production'` check) or the cross-site cookie/`trust proxy` logic stays off. Render free **cold-starts** (~30–60s) after ~15 min idle. **Caveat:** uploads use a free Supabase bucket; the throwaway `deploytest+…@example.com` user can be deleted from Neon.
- 2026-06-25 — **Deploy prep (FE-6) — full-stack free-tier path chosen & config landed.** FE→**Vercel**, API→**Render** (free), Postgres→**Neon** (free, persistent), uploads→**Supabase Storage** (S3-compatible, free). FE adds `vercel.json` (framework `vite` + SPA rewrite so deep links/refresh work). BE side (in `-BE`, `47260a7`): storage split into a `STORAGE_DRIVER` selector with a new S3-compatible driver (uploads survive Render's ephemeral disk; callers unchanged, unit-tested), prod cross-site cookies (`SameSite=None; Secure`) + `trust proxy`, `render.yaml` blueprint, and `DEPLOY.md` (step-by-step Neon→Supabase→Render→Vercel walkthrough + smoke test). Salary chip + clickable Source link also shipped (`7a21851`). **117/117 FE tests.**
- 2026-06-25 — **Application-details UX: job-description read view done & merged** (`f56f3c0`, pushed to `origin/main`). The cramped 3-row job-description `<textarea>` in `ApplicationDrawer` is replaced by a **faithful read view** (`whitespace-pre-wrap` + `leading-relaxed`) that preserves pasted line breaks and bullets exactly as copied from a posting (Indeed/LinkedIn/etc.), an **Edit** toggle to a tall (12-row) textarea, a dashed empty state, and an **Expand** button (`Maximize2`) opening a large reading modal (`max-w-2xl`, `max-h-[85vh]`). New apps open in edit mode. Escape closes only the modal, not the drawer (capture-phase listener + `stopPropagation`). The whole drawer widened `max-w-md → max-w-xl`. TDD: 3 new ApplicationDrawer tests → **109 total** + build. (Notes field intentionally left as the plain textarea for now.)
- 2026-06-24 — **V3-4 OpenRouter LLM layer (FE) done & merged** (`4c005f1`, `--no-ff`). On `/analysis`: a **"Use AI" toggle** (off by default; **disabled** when `GET /analysis/config` returns `aiAvailable:false`, with a hint to set an OpenRouter key) + a **consent line** (honest about free-model training data policy) shown when checked; `runAnalysis` sends `useAi`. `AnalysisReport` shows a **Sparkles AI badge** when `meta.aiUsed` and a **fallback note** when AI was requested but unavailable (`aiRequested && !meta.aiUsed`). New `src/api/analysis.js` `getAnalysisConfig()` (key `['analysisConfig']`). TDD: 3 AnalysisReport + 2 Analysis page = **106 total** + build; network mocked. Read-only review clean after one fix (render the checkbox `checked={useAi && aiAvailable}` so a config flip can't leave a checked-but-disabled box; aria-label aligned to the visible label).
- 2026-06-24 — **V3-3 Résumé Analysis / ATS (FE) done & merged** (`2af936c`, `--no-ff`). New `/analysis` page (sidebar, ScanSearch icon, after Activity): application + résumé pickers → "Run analysis" (`POST /analysis`) → an `AnalysisReport` (two `ScoreBadge`s — ATS-friendliness + match, color-banded, match shows N/A with no JD — ATS sub-score bars, matched/missing keyword chips with hard emphasis + JD frequency, severity-colored suggestions, a parseability-warning banner, and honest "guidance only — not a guaranteed ATS pass" copy); a no-JD note; a history list (open via `GET /:id`, delete). New `src/api/analysis.js`. TDD: 3 `AnalysisReport` + 4 `Analysis` page + 1 Layout = 8 → **101 total** + build. Read-only review caught a **blocker** (fixed): the no-JD note read `jobDescription` off the slim applications list (a detail-only field) so it always showed — now fetches the selected application's detail (`getApplication`); also clears the shown report when its analysis is deleted + surfaces open-history errors.
- 2026-06-24 — **V3-2 Activity Log (FE) done & merged** (`9a2f247`, `--no-ff`). New `/activity` feed page (`useInfiniteQuery(['activity'])`): events grouped by day ("Today"/"Yesterday"/date) with a "Load more" cursor button; loading/empty/error states. A per-application **Activity timeline** section in the application drawer (`useQuery(['activity', id])`). Shared `src/lib/activityCopy.js` (pure `(action, metadata) → {icon, text}` + `relativeTime`/`dayBucket`) + `ActivityRow` render the human sentence on the FE. New `src/api/activity.js`. The mutations that produce logged events — status save, delete, add-interview, set-result, link-contact, quick-create-contact, link-document, and the Kanban status move — invalidate `['activity']` so both surfaces refresh. TDD: 3 activityCopy + 4 Activity page + 1 Layout + 1 ApplicationDrawer tests → **93 total** + build. Read-only review clean; fixes: accessible `<time dateTime title>` rows + `Math.floor` relative durations.
- 2026-06-24 — **V3-1 Documents (FE) done & merged** (`6e3d4d0`, `--no-ff`). New `/documents` page (`useQuery(['documents', search])`): upload form (file + name + type + notes → multipart `POST /documents`), searchable library with type pills (Resume=sky, CoverLetter=emerald, Other=slate), **blob download** via the api client (Bearer-token aware → browser save), edit-metadata drawer (`DocumentDrawer`), delete; loading/empty/error states. New sidebar "Documents" nav item (FileText, after Contacts) + a **Documents section in the application drawer** (list linked, link existing, unlink) reusing the `['application', id]` detail query. New `src/api/documents.js` (list/create/update/delete/download/link/unlink). TDD: 7 Documents page + 1 DocumentDrawer + 1 Layout nav + 2 ApplicationDrawer tests → **84 total** + build. Read-only review clean; applied fixes: defer `URL.revokeObjectURL` past the anchor click (avoids intermittent empty downloads) + catch download failures into the page error alert. (Quick-upload-and-link inside the drawer deferred — upload lives on the page.)
- 2026-06-24 — **Reminders follow-ups (post-merge UI gaps surfaced while testing).** All merged to `main` (`--no-ff`), **73 tests** + build. (1) **Bugfix** `fix/interviews-list-query` (`71fb5ec`): the Interviews page passed `listInterviews` to `useQuery` by reference, so TanStack Query's context object leaked in as `applicationId` → axios bracket-serialized it → Prisma `Unknown argument 'client'` 500. Wrapped as `() => listInterviews()`; regression test asserts no `applicationId` param. (2) **Scheduled-date field** added to *both* interview-create forms — Interviews page (`c173391`) and the application drawer (`59fdd86`) — since neither exposed `scheduledAt` (BE always supported it), so no interview could ever land in Reminders' upcoming/overdue buckets. (3) **Result selector** (Pending/Passed/Failed) added to interview rows on the Interviews page (`ffb637e`) and the drawer (`121d1fa`) via `updateInterview` → `PATCH /interviews/:id`; recording Passed/Failed clears a past interview from the Overdue bucket. Note: result can't be cleared back to null (enum schema rejects null — would need a widen like contacts' `followUpDate`).
- 2026-06-23 — **V2-3 Reminders (FE) done & merged** (`497597c`, `--no-ff`, branch deleted). New `/reminders` page (`useQuery(['reminders'])`): grouped Interviews (overdue+upcoming) + Follow-ups (due+upcoming) cards, amber/sky pills per DESIGN.md, soft deep-links to `/interviews`/`/contacts`, loading/empty/error states, and a "Mark done" per follow-up reusing `updateContact(id,{followUpDate:null})` + invalidating `['reminders']`/`['contacts']`. New sidebar "Reminders" nav item (Bell, after Dashboard) with a count badge sharing the `['reminders']` query (one request, badge updates live). TDD: 5 page tests + Layout nav/badge tests → **69 total** + build. Read-only subagent review clean (no blockers). (Relaxed one plan test's exact `getByText('Backend Engineer')` to `/Backend Engineer/` since the spec'd subtitle renders "position · company" in one node — impl matches spec.)
- 2026-06-23 — FE-0…FE-5 implemented TDD on `feat/frontend-v1`. Design system in DESIGN.md.
- Caught during TDD: 401 interceptor was masking login credential errors with a failed token refresh — fixed to skip refresh on login/register/refresh 401s.
- Frontend dev server: http://localhost:5173 ; expects backend at http://localhost:4000.
